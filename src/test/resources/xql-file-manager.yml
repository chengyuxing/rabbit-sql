constants:
  ba-se: &basePath pgsql

filenames:
  - data.xql
  - system.xql

files:
  dt: !join [ *basePath, other.xql ]
  other: another.xql

delimiter: ;
charset: UTF-8
named-param-prefix:
pipes:
highlight-sql: false
check-period: 30
check-modified: false